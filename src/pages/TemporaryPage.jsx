function TemporaryPage({ title }) {
  return (
    <main className="grid h-full place-items-center rounded-[10px] bg-[var(--color-white)]">
      <h1 className="headline-2">{title}</h1>
    </main>
  );
}

export default TemporaryPage;
